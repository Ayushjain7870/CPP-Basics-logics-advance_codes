//DUPLICATE ELEMENTS
#include <stdio.h>
#include <conio.h>
#include <limits.h>
void main()
{
	int a[10] = {1,2,3,3,4,4,4,5,5,0},i,temp;
	for(i=0;i<(sizeof(a)/sizeof(a[0]));i++)
	{
		temp = a[i];
		if(temp);
	}
	printf("%d", (sizeof(a)/sizeof(a[0])));
}
